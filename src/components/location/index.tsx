import type { FC } from "react";

const Home: FC = () => {
  //const session = useSession();
  return (
    <div className=" flex h-screen bg-[#18181B]">
      <h1 className="swipe text-white">Location</h1>
    </div>
  );
};

export default Home;
